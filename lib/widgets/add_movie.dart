import 'package:flutter/material.dart';

class AddMovieView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Add new movie")),
      body: Text("implement me"),
    );
  }
}
